# راه‌اندازی ارسال فرم مشاوره به تلگرام

فرم مشاوره سایت الان به یک سرورلس‌فانکشن امن وصل شده (`api/contact.js`) که پیام رو به یک ربات تلگرام می‌فرسته. توکن ربات هیچ‌وقت توی کد فرانت‌اند نیست — فقط روی سرور Vercel و به‌صورت متغیر محیطی (Environment Variable) ذخیره می‌شه.

## مرحله ۱: ساخت ربات تلگرام

1. توی تلگرام برو سراغ [@BotFather](https://t.me/BotFather)
2. دستور `/newbot` رو بفرست
3. یک اسم و یک username (باید به `bot` ختم بشه، مثلاً `AisaClinicBot`) انتخاب کن
4. BotFather یک **توکن** بهت میده، شبیه این: `123456789:AAHn4xxxxxxxxxxxxxxxxxxxxxxxxxxxx` — این رو نگه‌دار

## مرحله ۲: پیدا کردن Chat ID

1. یک پیام دلخواه (مثلاً "سلام") به ربات تازه‌ساخته‌ات بفرست (باید اول با ربات استارت بزنی: `/start`)
2. این آدرس رو توی مرورگر باز کن (به‌جای `<TOKEN>` توکن خودت رو بذار):
   `https://api.telegram.org/bot<TOKEN>/getUpdates`
3. توی خروجی JSON دنبال `"chat":{"id":...}` بگرد — همون عدد، Chat ID توئه

> نکته: اگه می‌خوای پیام‌ها بیاد توی یک گروه یا کانال به‌جای چت خصوصی، ربات رو به اون گروه/کانال اضافه کن و همون‌جا پیام بده، بعد همین مرحله رو تکرار کن.

## مرحله ۳: دیپلوی و تنظیم Environment Variables

سایت الان دو راه دیپلوی داره — هرکدوم که استفاده می‌کنی رو دنبال کن:

### گزینه الف) Render (پیشنهاد فعلی)

روی Render باید به‌عنوان **Web Service** دیپلوی بشه، نه Static Site (چون فرم مشاوره به یک سرور کوچیک نیاز داره تا پیام رو امن به تلگرام بفرسته). فایل `server.js` همین سرور کوچیکه — بدون هیچ پکیج اضافه‌ای، فقط با ابزارهای خود Node.js نوشته شده تا نصبش روی Render ساده و بی‌دردسر باشه.

1. توی Render گزینه‌ی **New → Web Service** رو بزن و ریپازیتوری گیت‌هاب پروژه رو انتخاب کن
2. تنظیمات دیپلوی:
   - **Build Command:** خالی بذار (چیزی برای نصب نیست)
   - **Start Command:** `node server.js`
3. توی بخش **Environment** پروژه، این دو مقدار رو اضافه کن:
   - `TELEGRAM_BOT_TOKEN` = توکنی که از BotFather گرفتی
   - `TELEGRAM_CHAT_ID` = همون Chat ID
4. Deploy کن؛ چند لحظه بعد سایت روی آدرس `xxx.onrender.com` بالا میاد

> نکته: پلن رایگان Render بعد از ۱۵ دقیقه بی‌فعالیتی «می‌خوابه» و اولین درخواست بعدش چند ثانیه کندتره (Cold Start). برای یه سایت کلینیک با ترافیک معمولی معمولاً مشکلی ایجاد نمی‌کنه، ولی اگه می‌خوای همیشه بیدار بمونه، پلن پولی (Starter) این مشکل رو نداره.

### گزینه ب) Vercel (اگر بعداً خواستی برگردی)

1. پروژه رو به گیت‌هاب push کن و توی Vercel Import کن (فایل `api/contact.js` رو خودش به‌عنوان سرورلس‌فانکشن تشخیص می‌ده، نیازی به `server.js` نیست)
2. توی **Settings → Environment Variables** همون دو مقدار بالا رو اضافه کن
3. Redeploy کن

## مرحله ۴: تست

فرم مشاوره روی سایت رو با یک شماره تست پر کن و بفرست. باید ظرف چند ثانیه پیام توی تلگرام برات بیاد. اگه نیومد:
- مطمئن شو `/start` رو به ربات زده باشی
- مقادیر Environment Variables رو دوباره چک کن (فاصله یا کاراکتر اضافه نداشته باشن)
- روی Render: بخش **Logs** همون صفحه‌ی سرویس رو نگاه کن
- روی Vercel: بخش **Deployments → Functions → Logs** رو نگاه کن، خطا اونجا مشخص میشه

---

**نکته امنیتی:** این معماری (فرم → سرورلس‌فانکشن → تلگرام) استاندارده و توکن هیچ‌وقت در معرض دید کاربر یا داخل کد سایت قرار نمی‌گیره. یک هانی‌پات (فیلد مخفی ضد اسپم) هم توی فرم گذاشته شده که جلوی بخشی از پیام‌های رباتیک رو می‌گیره.
